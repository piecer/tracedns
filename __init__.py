"""Tracedns package placeholder.

This file exists so the repo root is exposed as a package named ``tracedns``
from its parent directory (e.g. ``from tracedns import alerts``). It is
intentionally empty — all real modules live one level down. See
``REFACTORING_GUIDE.md`` § "테스트" for the PYTHONPATH convention used by
``make test``.
"""
