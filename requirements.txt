requests==2.31.0
dnspython==2.3.0
PyMISP==2.4.140

# Optional packages (useful for local development / testing)
python-dotenv==1.0.0
pytest==7.3.1
coverage[toml]>=7.6,<8

# Optional: GeoIP (country code fallback when VT lacks coverage)
geoip2==4.8.0

# Note: VirusTotal lookups in `vt_lookup.py` use `requests` directly; no special VT package
# is required by default.

# Optional: ENS on-chain text record query helper.
# Installed separately because current PyMISP requires jsonschema<4 while web3/eth-utils
# requires jsonschema>=4. The default requirements file stays installable with PyMISP;
# enable ENS with: pip3 install --no-deps web3==6.20.4 eth-abi==5.0.1 eth-utils==4.2.1 eth-hash==0.5.3 eth-account==0.11
